.env
.work/
preview.html
vista-*.html

# Real customer reports and screenshots. These carry a client's company, plant,
# machine name and serial number, so they must never reach a public repository.
# The two regression tests that use them skip by themselves when they are absent.
Imagenes/
# Google Cloud service account keys. Never commit a credential file.
*-key.json
credentials*.json
service-account*.json
state/
archive/
logs/
__pycache__/
*.py[cod]
# Regenerated by `pip install -e .`
*.egg-info/
# Local tooling, not part of the project
.atl/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
